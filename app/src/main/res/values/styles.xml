<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <!--文件夹弹框的样式-->
    <style name="style_dir_dialog" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@style/dir_dialog_anim</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!--文件夹弹框动画样式-->
    <style name="dir_dialog_anim">
        <item name="android:windowEnterAnimation">@anim/enter_dialog_anim</item>
        <item name="android:windowExitAnimation">@anim/exit_dialog_anim</item>
    </style>
</resources>
